# backend/alembic.ini

# ... (other configurations) ...

# A string that indicates how to connect to the database.
# For a real application, this should probably come from an environment variable
# or a configuration file specific to your deployment strategy.
# We will configure this dynamically in env.py using our app.config.settings
sqlalchemy.url = postgresql://user:password@host:port/database_name